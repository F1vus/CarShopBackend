CREATE TABLE IF NOT EXISTS auto_producent (
                                              id SERIAL PRIMARY KEY,
                                              auto_produces VARCHAR(255) NOT NULL
    );
